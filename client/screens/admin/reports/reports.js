import * as React from "react";
import { Text, View, Image, Pressable, ScrollView } from "react-native";
import styles from "./styles";

const Reports = () => {
  return (
    <ScrollView>
      <View>
        <Text>este es el espacio para notificaciones</Text>
      </View>
    </ScrollView>
  );
};

export default Reports; 
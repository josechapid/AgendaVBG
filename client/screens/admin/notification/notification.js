import * as React from "react";
import { Text, View, Image, Pressable, ScrollView } from "react-native";
import styles from "./styles";

const Notification = () => {
  return (
    <ScrollView>
      <View>
        <Text>este es el espacio de notificaciones</Text>
      </View>
    </ScrollView>
  );
};

export default Notification; 
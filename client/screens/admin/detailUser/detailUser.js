import * as React from "react";
import { Text, View, Image, Pressable, ScrollView } from "react-native";
import styles from "./styles";

const DetailUser = () => {
  return (
    <ScrollView>
      <View>
        <Text>este es el detalle del usuario buscado por el admin</Text>
      </View>
    </ScrollView>
  );
};

export default DetailUser; 
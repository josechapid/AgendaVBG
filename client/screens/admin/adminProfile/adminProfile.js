import * as React from "react";
import { Text, View, Image, Pressable, ScrollView } from "react-native";
import styles from "./styles";

const AdminProfile = () => {
  return <ScrollView>
    <View>
        <Text>
            este es el perfil del admin 
        </Text>
    </View>
  </ScrollView>;
};

export default AdminProfile; 
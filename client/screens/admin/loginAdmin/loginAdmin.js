import * as React from "react"
import {Text, View, Image, Pressable, ScrollView} from "react-native"
import styles from "./styles"

const LoginAdmin = () =>{
    return (
      <ScrollView>
        <View>
          <Text>esta es la vista para registrar un admin</Text>
        </View>
      </ScrollView>
    );
}

export default LoginAdmin 